function mostrar()
{
	var edad;

	edad =  txtIdEdad.value;
	edad = parseInt(edad);

	if (edad == 15) 
	{
		alert("niña bonita");
		
	}

}//FIN DE LA FUNCIÓN

/*operadores:
aritmeticos: +, -, *, /, %
relaciones: >, <, >=, <=, !=, 
logicos: && (y), || (o), ! (no)*/ 